## web-blog-with-boostrap

## Usage

1. Fork the repo and then clone it or download it.

2. First install all dependencies:
	```
		# with npm
		npm install
	```
3. Now run the web
	```
		live-server
	```